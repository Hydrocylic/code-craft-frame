# <线头> — 派工单 <YYYY-MM-DD>

<!--
  模板: _framework/templates/dispatch-order.md
  适用位置: notes/agents/dispatch/<线头>-<YYYY-MM-DD>.md（见 notes/spec/agent-dispatch.md §2）
  整理方: <agent ID>（名册 notes/agents/roster.md）
  用法: 用户按 §2 逐行点名 agent；§3 的 kickoff 段落可整段粘贴开新对话；被派方只回填 §2 的状态列
  命名: 线头用**主题**不用项目路径（例 webgl-next-phase-2026-09-17.md）
-->

## 0. 状态速览

<!-- 与 notes/agents/roster.md 的状态保持一致：一条线一行，写清"缺什么" -->

| 线 / 目标 | 状态 | 缺什么 |
|---|---|---|
| <线头 1> | <就绪 / 等数据 / 已授权…> | <缺的东西，或 —> |

**已定环境 / 前置**：<一句话——本批任务共用的既有结论、设备、口径>

## 1. 通用交接纪律（粘给任何新 agent 的开头）

```
你在 <工作区> 工作。先读 <仓根入口文件>（如 CLAUDE.md / AGENTS.md），并读 notes/agents/roster.md ——
按其中约定自报你的 ID（<job>-<YYMMDD>，日期用本次对话创建日）+ 服务对象 + 写入范围；
文档里自指一律用该 ID，不写"本 agent"。
硬约束：<本仓的不可写区与只读区，逐条列清>；不执行任何 git 指令（含只读查询）；
联网获取类先写计划 md 等用户执行；依赖安装与构建由用户执行；
图片只读属性元数据、不读内容；脚本只在无头可验且必要时跑，方法落成可复用脚本或记入 log；
每次会话结束追加时间戳执行日志条目（做了什么 / 关键决策 / 产生文件 / 下一步）。
```

## 2. 派工表

| # | 任务 | 建议执行方 | 前置 / 状态 | 交付物 | 验收判据 | 阻塞谁 |
|---|---|---|---|---|---|---|
| **1** | <任务一句话> | `<job>-*` | <前置条件 / 是否已授权> | <产物与落点> | <可判定的验收条件> | <阻塞哪条线，或 否> |

## 3. 可直接粘贴的 kickoff（按 §2 编号）

**① <任务名>**

```
你在 <工作区> 工作。先读 <入口文件> 与 notes/agents/roster.md（自报 ID + 服务对象 + 写入范围），再读：
1. <必读文件，带章节或行号>
任务：<一句话——做什么，不做什么>
交付：<产物与落点>
边界：<不能动什么（代码 / 路径 / 口径）>
验收：<可判定条件；能对拍的给数值或对拍方式>
```

## 4. 待拍板项

| # | 事项 | 阻塞什么 |
|---|---|---|
| — | <需要用户决定的事> | <不决定就卡住的东西> |

## 5. <整理方 ID> 自办

- <整理方自己接着做的事——不派出去的计划、索引、回填>
- **状态更新纪律**：任务状态变化时回填 §2 与头部「更新」日期，并同步 `notes/agents/roster.md` 对应行。

## 6. 移交说明（原执行线归档时填）

<!--
  何时填：本单的执行线归档（或该批任务阶段性结束）时。
  要点：剩余任务并入哪一轮 / 由谁承接 / 本单自此只读 / 接手方另开新单而非原地改写。
-->

- **背景**：<原执行线 ID> 已于 <日期> 归档（交接件：<路径>）。
- **剩余任务去向**：<并入 <轮次/任务单元>，由 <新线 ID> 承接并自行派发排序>。
- **本单自此只读**；接手方要更新状态请**另开** `notes/agents/dispatch/<新线头>-<日期>.md`。
